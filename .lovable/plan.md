## Mobile Market Detail — Compact Sticky Purchase Bar (COMPLETED)

Sticky bar split into two zones:
- **Inline section** (scrollable): Quick ticket pills, Weekly Draw badge, summary card, pot split bar — visible when scrolling through market content
- **Compact sticky bar** (~120px): Outcome buttons + ticket stepper + buy button + info line — always visible at bottom

Desktop panel unchanged.
